# =============================================================================
# HiClaw Workers — Stateless agent containers
# =============================================================================

apiVersion: apps/v1
kind: Deployment
metadata:
  name: hiclaw-graph-worker
  namespace: honeybadge
  labels:
    app.kubernetes.io/name: hiclaw-graph-worker
    app.kubernetes.io/component: worker
    app.kubernetes.io/part-of: honeybadge
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: hiclaw-graph-worker
  template:
    metadata:
      labels:
        app.kubernetes.io/name: hiclaw-graph-worker
        app.kubernetes.io/component: worker
        app.kubernetes.io/part-of: honeybadge
    spec:
      containers:
        - name: graph-worker
          image: higress-registry.cn-hangzhou.cr.aliyuncs.com/higress/hiclaw-worker:v1.1.2
          command: ["/bin/bash", "/opt/honeybadge/init/worker-init-wrapper.sh"]
          env:
            - name: HICLAW_WORKER_NAME
              value: "graph-worker"
            # v1.1.0 split topology: MinIO moved from manager Pod to embedded Pod
            - name: HICLAW_FS_ENDPOINT
              value: "http://hiclaw-embedded:9000"
            - name: HICLAW_FS_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: honeybadge-secrets
                  key: HICLAW_ADMIN_USER
            - name: HICLAW_FS_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: honeybadge-secrets
                  key: HICLAW_ADMIN_PASSWORD
            - name: HICLAW_REGISTRATION_TOKEN
              valueFrom:
                secretKeyRef:
                  name: honeybadge-secrets
                  key: HICLAW_REGISTRATION_TOKEN
            - name: HICLAW_MATRIX_DOMAIN
              value: "matrix-local.hiclaw.io"
            - name: TZ
              value: "Asia/Shanghai"
            - name: NODE_OPTIONS
              value: "--max-old-space-size=1536"
          resources:
            requests:
              memory: "512Mi"
              cpu: "200m"
            limits:
              memory: "2Gi"
              cpu: "1"
          volumeMounts:
            - name: init-scripts
              mountPath: /opt/honeybadge/init/worker-init-wrapper.sh
              subPath: worker-init-wrapper.sh
              readOnly: true
      volumes:
        - name: init-scripts
          configMap:
            name: hiclaw-init-scripts
            defaultMode: 0755
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: hiclaw-analytics-worker
  namespace: honeybadge
  labels:
    app.kubernetes.io/name: hiclaw-analytics-worker
    app.kubernetes.io/component: worker
    app.kubernetes.io/part-of: honeybadge
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: hiclaw-analytics-worker
  template:
    metadata:
      labels:
        app.kubernetes.io/name: hiclaw-analytics-worker
        app.kubernetes.io/component: worker
        app.kubernetes.io/part-of: honeybadge
    spec:
      containers:
        - name: analytics-worker
          image: higress-registry.cn-hangzhou.cr.aliyuncs.com/higress/hiclaw-worker:v1.1.2
          command: ["/bin/bash", "/opt/honeybadge/init/worker-init-wrapper.sh"]
          env:
            - name: HICLAW_WORKER_NAME
              value: "analytics-worker"
            # v1.1.0 split topology: MinIO moved from manager Pod to embedded Pod
            - name: HICLAW_FS_ENDPOINT
              value: "http://hiclaw-embedded:9000"
            - name: HICLAW_FS_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: honeybadge-secrets
                  key: HICLAW_ADMIN_USER
            - name: HICLAW_FS_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: honeybadge-secrets
                  key: HICLAW_ADMIN_PASSWORD
            - name: HICLAW_REGISTRATION_TOKEN
              valueFrom:
                secretKeyRef:
                  name: honeybadge-secrets
                  key: HICLAW_REGISTRATION_TOKEN
            - name: HICLAW_MATRIX_DOMAIN
              value: "matrix-local.hiclaw.io"
            - name: TZ
              value: "Asia/Shanghai"
            - name: HERMES_MAX_TURNS
              value: "25"
            - name: NODE_OPTIONS
              value: "--max-old-space-size=1536"
          resources:
            requests:
              memory: "512Mi"
              cpu: "200m"
            limits:
              memory: "2Gi"
              cpu: "1"
          volumeMounts:
            - name: init-scripts
              mountPath: /opt/honeybadge/init/worker-init-wrapper.sh
              subPath: worker-init-wrapper.sh
              readOnly: true
            - name: init-scripts
              mountPath: /opt/honeybadge/init/hermes-config-bridge.sh
              subPath: hermes-config-bridge.sh
              readOnly: true
      volumes:
        - name: init-scripts
          configMap:
            name: hiclaw-init-scripts
            defaultMode: 0755
