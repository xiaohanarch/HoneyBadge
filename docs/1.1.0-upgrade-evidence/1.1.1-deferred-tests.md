# 1.1.1 Deferred E2E Tests Tracker

**Status**: Updated post-TC-110/111/305 reactivation. The two-stage harness wait
(`_wait_for_new_response` → `_wait_for_response_settled` in `tests/e2e/conftest.py`)
is landed. ~30 formerly-skipped Category A/B/C/G tests are now unskipped and active;
the chat baseline (9/10 pass) confirms the harness captures the Worker contract-002
response rather than the Manager dispatch ack.

TC-110/111/305 (the pytest-timeout deadlock class + the TC-305 teardown race) are
now resolved via per-test `@pytest.mark.timeout(600)`, the `reset_manager` fixture,
and a `create_user_page` rewrite that mirrors TC-308.

This tracker now reflects only the **remaining** skips — Category E items that are
out of scope for the current E2E baseline effort and tracked here for the 1.1.1
milestone.

---

## Remaining Skip Inventory

### `tests/e2e/test_01_auth.py` — 1 skip

| Test | Category | Notes |
|------|----------|-------|
| `test_tc005_logout_redirects_to_login` | E | `<el-dropdown>` hover on nested `.user-avatar span` does not reveal the teleported menu. Element Plus v2.9 default trigger is hover; propagation through the avatar wrapper is inconsistent. Fix: switch to `click` trigger or programmatic dropdown open. |

### `tests/e2e/test_03_session.py` — 1 skip

| Test | Category | Notes |
|------|----------|-------|
| `test_tc205_session_persistence_across_reload` | E | After `page.reload()`, `wait_for_chat_ready()` times out at 20s because the Pinia chat store doesn't reach `connected=true`. Fix: wait for `page.evaluate(() => window.matrixClient?.isInitialSyncComplete?.())` before asserting. |

### `tests/e2e/test_05_permissions.py` — 1 skip

| Test | Category | Notes |
|------|----------|-------|
| `test_tc407_*` | E | Transient backend disconnect (`httpx.RemoteProtocolError: Server disconnected without sending a response`). Backend instability under load; may resolve with a retry or backend connection-pool fix. |

### `tests/e2e/test_02_chat.py` — RESOLVED (0 skips)

TC-110/111 deadlock resolved without an async-API migration:
- TC-110: `@pytest.mark.timeout(600)` gives 600s headroom for 3 queries × ~60s
  (~180s with a fresh Manager), so pytest-timeout never fires mid-query and the
  thread-kill deadlock is avoided. `reset_manager` prevents the glm-5.2 repetition
  loop (5-10 accumulated turns) from stalling Stage-1.
- TC-111: removed the Windows-only `skipif` — the deadlock concern only applies
  when pytest-timeout interrupts a long-running multi-query test. A single ~60-90s
  query is well within the 600s safety timeout.

### `tests/e2e/test_04_isolation.py` — RESOLVED (0 skips)

TC-305 rewritten to use `create_user_page` (separate browser contexts per user)
instead of single `page` + `localStorage.clear()` in-place user switching. The
Matrix SDK JS state is not reset by `localStorage.clear()`, which caused navigation
races and a teardown ERROR when `page.close()` ran on a destroyed execution
context. The new body mirrors TC-308 and only asserts `localStorage.auth_store`
differs per user — no chat queries needed.

### `tests/e2e/test_07_mcp.py` — RESOLVED (0 skips)

Category F (MCP connectivity) + B (backend cascade) — all blockers resolved.
Module-level skip removed. All 10 tests pass (TC-601-610).

---

## Resolved Categories (kept for history)

- **A / B / C / G** — Mid-stream LLM preamble read. FIXED via the two-stage
  harness wait in `conftest.py`: Stage 1 waits for any new assistant message,
  Stage 2 settles on (cond1) a structured Worker reply with a fresh trace_id,
  (cond2) a denial marker, or (cond3) text stable for 10s + 5s grace period.
  Trace_id timestamp parsing filters stale backfilled DM history. Chat baseline
  confirms TC-102/105/106/107/108/109 pass; antihal TC-503–513 are active.
- **D** — Matrix room provisioning. FIXED in PR #110 (TC-308 passing).
- **H** — NebulaGraph empty result rows. Resolved via seed/migration parity
  (TC-105/504 return data rows).
- **TC-110/111 pytest-timeout deadlock** — FIXED via per-test
  `@pytest.mark.timeout(600)` + `reset_manager` fixture. No async-API migration
  needed; the larger headroom means the timeout never fires mid-query.
- **TC-305 teardown race** — FIXED via `create_user_page` rewrite mirroring TC-308.

---

## Reactivation Checklist (remaining items)

1. **Category E** — fix individually (each is a distinct sub-issue):
   - TC-005: switch el-dropdown to `click` trigger or programmatic open.
   - TC-205: wait for Matrix initial sync before asserting chat readiness.
   - TC-407: stabilize backend under load (transient — may just need retry/backoff).
2. ~~**TC-110/111 deadlock**~~ — RESOLVED via per-test timeout + `reset_manager`.
3. ~~**TC-305 teardown race**~~ — RESOLVED via `create_user_page` rewrite.
4. ~~**Category F (test_07_mcp)**~~ — RESOLVED. Module-level skip removed,
   all 10 tests pass.
5. Delete this tracker doc as the final step of the 1.1.1 milestone once the
   above Category E items are resolved or re-triaged.
