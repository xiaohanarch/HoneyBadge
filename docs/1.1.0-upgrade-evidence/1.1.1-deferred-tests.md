# 1.1.1 Deferred E2E Tests Tracker

**Status**: Updated post-TC-005 reactivation + TC-205 re-triage. The two-stage
harness wait (`_wait_for_new_response` → `_wait_for_response_settled` in
`tests/e2e/conftest.py`) is landed. ~30 formerly-skipped Category A/B/C/G tests
are now unskipped and active; the chat baseline confirms the harness captures the
Worker contract-002 response rather than the Manager dispatch ack.

TC-110/111/305 (pytest-timeout deadlock + teardown race) and TC-005 (el-dropdown)
are resolved. TC-205 was re-triaged: the original "chat readiness timing" diagnosis
was wrong — the real blocker is a feature gap (no message persistence), tracked
below.

This tracker now reflects only the **remaining** skips — Category E / feature-gap
items that are out of scope for the current E2E baseline effort and tracked here
for the 1.1.1 milestone.

---

## Remaining Skip Inventory

### `tests/e2e/test_03_session.py` — 1 skip

| Test | Category | Notes |
|------|----------|-------|
| `test_tc205_session_persistence_across_reload` | feature gap | **Not a timing issue.** After `page.reload()`, `ChatView.onMounted` calls `loadMessages(sessionId)` which GETs `/api/sessions/{id}/messages` — but the `chat_messages` table is never populated. The chat hot path uses Matrix (in-memory Pinia store only); `handleSend → sendQuery` sends via Matrix and the `Room.timeline` handler updates the Pinia store but never persists to the backend. `sessions.py` has a `chat_messages` table + GET endpoint but NO write path. Sessions ARE persisted (handleNewChat POSTs `/api/sessions`); messages are NOT. To unskip: implement message persistence — either a POST `/api/sessions/{id}/messages` endpoint + frontend hook on message receive, or a Matrix-side writer. |

### `tests/e2e/test_05_permissions.py` — 1 skip

| Test | Category | Notes |
|------|----------|-------|
| `test_tc407_*` | E / missing endpoint | The test GETs `/api/admin/users` expecting 401/403, but **this endpoint does not exist** in the backend (`src/honeybadge/server/` only has `/api/auth/*`, `/api/sessions/*`, `/api/audit/*`, `/health`). The skip reason's `RemoteProtocolError` is a symptom; the deeper issue is testing a non-existent feature. Fix options: implement an admin-only endpoint, rewrite the test to target an existing role-enforced endpoint, or delete the test. |

### `tests/e2e/test_01_auth.py` — RESOLVED (0 skips)

TC-005 fixed via three coordinated changes:
- Frontend: `ChatView.vue` user-avatar `<el-dropdown>` given `trigger="click"`
  (was default `hover`, inconsistent with the session dropdown on the same view
  which already used `trigger="click"`).
- Selector: `LOGOUT_ITEM` corrected from `.el-dropdown-item` to
  `.el-dropdown-menu__item` — Element Plus v2.9 renders dropdown items as
  `<li class="el-dropdown-menu__item">`, not `.el-dropdown-item`. The old selector
  matched zero elements in the DOM.
- Test: `.hover()` → `.click()`.

### `tests/e2e/test_02_chat.py` — RESOLVED (0 skips)

TC-110/111 deadlock resolved without an async-API migration:
- TC-110: `@pytest.mark.timeout(600)` gives 600s headroom for 3 queries × ~60s
  (~180s with a fresh Manager), so pytest-timeout never fires mid-query and the
  thread-kill deadlock is avoided. `reset_manager` prevents the glm-5.2 repetition
  loop (5-10 accumulated turns) from stalling Stage-1.
- TC-111: removed the Windows-only `skipif` — the deadlock concern only applies
  when pytest-timeout interrupts a long-running multi-query test. A single ~60-90s
  query is well within the 600s safety timeout.

### `tests/e2e/test_04_isolation.py` — RESOLVED (0 skips)

TC-305 rewritten to use `create_user_page` (separate browser contexts per user)
instead of single `page` + `localStorage.clear()` in-place user switching. The
assertion checks `localStorage.token` (per-user roles_jwt) — the originally-planned
`auth_store` key does not exist (Pinia auth store is in-memory only, no persist
plugin; auth data is written as individual keys by `useAuth.ts`).

### `tests/e2e/test_07_mcp.py` — RESOLVED (0 skips)

Category F (MCP connectivity) + B (backend cascade) — all blockers resolved.
Module-level skip removed. All 10 tests pass (TC-601-610).

---

## Resolved Categories (kept for history)

- **A / B / C / G** — Mid-stream LLM preamble read. FIXED via the two-stage
  harness wait in `conftest.py`: Stage 1 waits for any new assistant message,
  Stage 2 settles on (cond1) a structured Worker reply with a fresh trace_id,
  (cond2) a denial marker, or (cond3) text stable for 10s + 5s grace period.
  Trace_id timestamp parsing filters stale backfilled DM history.
- **D** — Matrix room provisioning. FIXED in PR #110 (TC-308 passing).
- **H** — NebulaGraph empty result rows. Resolved via seed/migration parity.
- **TC-110/111 pytest-timeout deadlock** — FIXED via per-test
  `@pytest.mark.timeout(600)` + `reset_manager` fixture.
- **TC-305 teardown race** — FIXED via `create_user_page` rewrite mirroring TC-308.
- **TC-005 el-dropdown** — FIXED via `trigger="click"` + `LOGOUT_ITEM` selector
  correction (`el-dropdown-menu__item`).

---

## Reactivation Checklist (remaining items)

1. **TC-205 (feature gap)** — implement message persistence. The `chat_messages`
   table exists but is never written to. Requires either a POST
   `/api/sessions/{id}/messages` endpoint + frontend hook, or a Matrix-side
   writer. Not a test-only fix.
2. **TC-407 (missing endpoint)** — `/api/admin/users` does not exist. Decide:
   implement an admin-only endpoint, rewrite the test against an existing
   role-enforced endpoint, or delete the test.
3. ~~**TC-005**~~ — RESOLVED via `trigger="click"` + selector fix.
4. ~~**TC-110/111 deadlock**~~ — RESOLVED via per-test timeout + `reset_manager`.
5. ~~**TC-305 teardown race**~~ — RESOLVED via `create_user_page` rewrite.
6. ~~**Category F (test_07_mcp)**~~ — RESOLVED. Module-level skip removed.
7. Delete this tracker doc as the final step of the 1.1.1 milestone once the
   above items are resolved or re-triaged.
